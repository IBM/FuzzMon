const
	myObject = {
		12: 2,
		13: 3
	};

function calcThings(a, b) {
	return a + b;
}

async function
testFunc(



	input
) {
	var a = 3,
		inputReference = input;
	const myObjectReference = myObject;
	while (1 > 2) console.log('hi there!');
	if (-Infinity <
		a) {
		{
			{
				inputReference += a;
			}
		}
	}
	if (2<3) {
		a = (() => {
			return a + 0;
		})();		
		a = (() => 
			0 + a
		)();
		let b = a + await (calcThings(a+9)); // << add instrumentation before the call to calcThings()
	}
	if (a < inputReference);
	for (let i in
			myObjectReference) {
		if (i === 12) continue
		inputReference += myObjectReference[i];
	}
	if (a > inputReference)
		return -
			Infinity;
	else if (inputReference > inputReference)
		return Infinity
	else return inputReference;
}

module.exports = testFunc;